**Prompt:**

Please improve the **Dashboard** by enhancing the **Revenue by Carrier** section **without changing the application's design, layout, or existing functionality**.

## Display Expenses by Carrier

Currently, the Dashboard only displays the **Revenue** generated by each carrier.

I would also like it to display the **Total Expenses** associated with each carrier for the selected period.

### Requirements

For every carrier shown in this section, display:

* Carrier Name
* Total Revenue
* Total Expenses

All values must respect the currently selected date filter.

### Data calculation

The **Total Expenses** should be calculated using all expense records associated with freights belonging to each carrier.

Whenever:

* a freight is created,
* an expense is added,
* a freight or expense is edited,
* a freight or expense is deleted,

the Dashboard must automatically recalculate both **Revenue** and **Expenses** for every carrier.

### Date filtering

The displayed values must always respect the selected period.

For example:

* Today
* This Week
* This Month
* This Year
* Custom Date Range

Changing the date filter should immediately update both Revenue and Expenses.

### User Interface

Keep the existing design and simply expand the information shown for each carrier.

For example:

| Carrier   |     Revenue |   Expenses |
| --------- | ----------: | ---------: |
| Carrier A | €125,430.00 | €48,750.00 |
| Carrier B |  €92,100.00 | €35,420.00 |
| Carrier C |  €58,900.00 | €21,180.00 |

Maintain the current typography, colors, spacing, and visual style.

### Performance

* Calculate the values efficiently.
* Avoid unnecessary database queries.
* Ensure the information updates in real time whenever freight or expense data changes.
* The calculation should use the complete dataset, not only the records currently visible in the Freights or Expenses pages.

## Important

* Do **not** redesign the Dashboard.
* Do **not** modify unrelated widgets or charts.
* Preserve the existing layout.
* Simply enhance the **Revenue by Carrier** section by including the **Total Expenses** for each carrier, always synchronized with the selected date range and the latest application data.
